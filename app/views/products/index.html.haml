%h1 Listing products

%table
  %thead
    %tr
      %th Title
      %th Description
      %th Image url
      %th Price
      %th{colspan:3} Action

  %tbody
    - @products.each do |product|
      %tr{class:cycle('list_line_odd','list_line_even')}
        %td= product.title
        %td.list_description= truncate(product.description,length:80)
        %td= image_tag(product.image_url,class:'list_image')
        %td= product.price
        %td= link_to 'Show', product
        %td= link_to 'Edit', edit_product_path(product)
        %td.list_action= link_to 'Destroy', product, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Product', new_product_path
